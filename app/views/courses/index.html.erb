<div class="row">
	<h1>Code Fellows Courses</h1>
</div>

<div class="row">
	<% if @courses.empty? %>
	Sorry, there aren't any courses here yet...
	<% else %>
	<table id="courses" class="table">
		<thead>
			<tr>
				<th>Title</th>
		</thead>
		<tbody class="list">
			<%= render @courses %>
		</tbody>
	</table>
	<% end %>
</div>
