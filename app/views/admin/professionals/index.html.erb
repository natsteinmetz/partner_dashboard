<div class="row-fluid">
  <h1>Code Fellows Company Professionals</h1>
</div>

<div class="row-fluid">
  <% if @professionals.empty? %>
  Sorry, there aren't any professionals here yet...
  <% else %>
  <table id="professionals" class="table index">
    <thead>
      <tr>
        <th>Partner Name</th>
        <th>Professional Name</th>
        <th>Professional Email</th>
        <th>Professional Phone Number</th>
        <th class="input-prepend">
          <span class="add-on"><i class="icon-filter"></i></span>
          <input id="professional-filter" type="text" class="search input-medium" placeholder="Filter Professionals" />
        </th>
      </tr>
    </thead>
    <tbody class="list">
      <% @professionals.each do |professional| %>
        <tr>
          <td class="partner_name"><%= link_to professional.partner.name, partner_path(professional.partner) %></td>
          <%# needs to be updated after profiles are created %>
          <td class="professional_name"><%= link_to professional.profile.name, professional_path(professional) %></td>
          <td class="professional_email"><%= professional.email %></td>
          <td class="professional_phone"><%= professional.profile.phone_number %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>
</div>

<script>
  var options = {
    valueNames: ['partner_name', 'professional_name', 'professional_email', 'professional_phone']
  };

  var professionalsList = new List('professionals', options);
</script>
